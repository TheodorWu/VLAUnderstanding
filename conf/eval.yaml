defaults: # Using mostly empty defaults to avoid unnecessary dependencies
 - perturbator: empty
 - wandb: default
 - activation_writer: default
 - activation_reader: empty
 - dataset: libero
 - model: empty
 - method: eval
 - evaluator: default

stage: prod
seed: 42
device: cuda
mode: evaluate
hydra:
  output_subdir: null
